/**
 * sound/levels.ts - громкости мира в одном месте.
 *
 * Всё в децибелах относительно мастера: у постоянных слоёв это RMS, у
 * событий - пик. Это отправная точка сведения, а не приговор: итог слушается
 * в браузере последним шагом, и правится он здесь, а не по файлам синтеза.
 *
 * У источника с местом уровень задан на опорном расстоянии `ref`, дальше он
 * падает по обратному закону (`place.ts`).
 */

import type { DoorId } from '../world/zones'

export const LEVEL = {
  /** Дождь по листьям на поляне: густой, везде одинаковый. */
  rain: -18,
  /** Гул лампы у двери, на 1 м. */
  lamp: { db: -34, ref: 1 },
  /** Генератор в своей комнате, на полутора метрах. */
  generator: { db: -26, ref: 1.5 },
  /**
   * Сколько отнимает закрытая стена, и где срезает верх. Генератор за стенами
   * выходит на -26 - 12 = -38 дБ у самой стены.
   */
  wall: { db: -12, cutoff: 300 },
  /** Насекомые: общий уровень шины, где бы ни стоял слушатель. */
  insects: -30,
  /** Шаг, пик. */
  step: -24,
  /** Далёкий гром и близкий удар, пик. */
  thunderFar: -12,
  thunderNear: -3,

  // Дальше - слои, которых нет в общей таблице уровней: числа мира, отправные.

  /** Дождь по бетону и лужам у поста: на опоре от стен и навеса. */
  rainConcrete: { db: -27, ref: 1.5 },
  /** Завеса капель с кромки навеса, на 1 м от кромки. */
  curtain: { db: -30, ref: 1 },
  /** Струя водостока в лужу, на 1 м. */
  gutter: { db: -27, ref: 1 },
  /** Ручей у западного края, на 2 м. */
  stream: { db: -31, ref: 2 },
  /** Редкие события на опушке: пик на опорном расстоянии. */
  bird: { db: -24, ref: 10 },
  creak: { db: -26, ref: 8 },
  fruit: { db: -22, ref: 4 },
  /** Одиночная капля на 1 м, пик. */
  drip: { db: -30, ref: 1 },

  // --- Нутро ---------------------------------------------------------------------

  /** Дробь дождя по своду и по плите крыши изнутри: всюду под своей кровлей одинаково. */
  roof: -24,
  /** Гул трубки в комнате: на полутора метрах - трубка висит над головой. */
  tube: { db: -38, ref: 1.5 },
  /** Фитолампы выше и тоньше обычных трубок, и тише. */
  phyto: { db: -42, ref: 1 },
  /** Зуд стартёра у мигающей трубки и его щелчок на провале, пик. */
  starter: { db: -48, ref: 1.5 },
  starterTick: { db: -40, ref: 1.5 },
  /** Часы: пик тиканья на 1 м. */
  clock: { db: -36, ref: 1 },
  /**
   * Приёмник: шипение на 1 м. Морзянка в эфире на 6 дБ громче шипения
   * (-30 дБ), свист гетеродина - на 8 дБ тише.
   */
  radio: { db: -36, ref: 1 },
  /** Пульт: гудение и писк строчной развёртки экрана, очень тихо. */
  console: { db: -46, ref: 1 },
  screen: { db: -58, ref: 1 },
  /** Капля в переполненное ведро и в раковину, пик на 1 м. */
  bucket: { db: -30, ref: 1 },
  sink: { db: -36, ref: 1 },
  /** Насосы нижнего яруса. */
  pump: { db: -30, ref: 2 },
  /** Машины за дверью холодной (в самой холодной; за дверью - через её потерю). */
  cold: { db: -20, ref: 2 },
  /** Стон корпуса, пик. */
  groan: { db: -22, ref: 4 },
  /** Дверь: удар створки о коробку и лязг замка, пик на 1 м. Скрип тише. */
  door: { db: -8, ref: 1 },
  hinge: { db: -14, ref: 1 },
  /** «Вздох» давления на пороге входной двери, пик. */
  sigh: -18,
} as const

/**
 * Затенение по графу зон, а не по лучам (sound.md, «Затенение источников»):
 * в той же зоне - полная полоса; через открытый проём - срез 800-1500 Гц;
 * через закрытую дверь или стену - срез 200-400 Гц и потеря 12-20 дБ. Доля
 * открытия двери смешивает два случая.
 */
export const SHADE = {
  /** Срез через один открытый проём, Гц; каждый следующий проём по пути ниже в `hop` раз. */
  open: 1000,
  hop: 0.8,
  /** Срез через закрытую дверь или стену, Гц, и нижний предел среза вообще. */
  closed: LEVEL.wall.cutoff,
  floor: 200,
  /**
   * Свой срез закрытой двери, если полотно пропускает больше стены. У входной
   * окошко и щели старой коробки: закрытая, она пропускает дождь до 1 кГц
   * (sound.md, тамбур: «дождь снаружи через дверь, срез 1 кГц»). Внутренняя
   * дверь тамбура глухая, и дальше тамбура улица слышна как через стену.
   */
  leaky: { entry: 1000 } as Partial<Record<DoorId, number>>,
  /** Потеря по видам преград, дБ: стена, стальная дверь, крышка люка, утеплённая дверь, перекрытие. */
  loss: { wall: LEVEL.wall.db, door: -12, hatch: -14, cold: -16, slab: -20 },
  /** Постоянная, с которой едет фильтр затенения, с. */
  tau: 0.15,
} as const

/**
 * Сколько сухого звука уходит в свёртку пространства. Дальний источник звучит
 * отражениями больше, чем прямым звуком, - посыл ему дают больше.
 */
export const SEND = {
  near: 0.35,
  far: 0.8,
  /**
   * Ровный тон (гул лампы и трубок, пульта, машин холодной) в свёртку не
   * посылается: отклик ровного тона - тот же тон со сдвигом фазы, комнаты он
   * не добавляет, а складываясь с прямым звуком, гуляет его громкостью от
   * частоты и места (снято счётом: гул трубки в посту терял 1.1 дБ). Комнату
   * слышно по разовым звукам и шумам.
   */
  tone: 0,
} as const
