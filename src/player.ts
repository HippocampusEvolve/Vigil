/**
 * player.ts — числа тела этого мира.
 *
 * Формула движения общая, из ядра (`world-core/core`, `Body` и `SmoothLook`),
 * и здесь не меняется ни строкой. Миру достаются только числа, и они про
 * походку: тело немолодое и тяжёлое, шаг короткий, бег - рывок на несколько
 * секунд, прыжок - подскок. Обзор чуть теснее, чем в зимних мирах, дыхание
 * в покое заметнее.
 *
 * Числа живут одним модулем, потому что их спрашивают трое: тело в `main.ts`,
 * взгляд на лампу в `world/index.ts` и внешние проверки проходимости.
 */

export const BODY = {
  /** Высота глаза над ступнёй, м. */
  eye: 1.62,
  /** Высота капсулы тела, м. */
  height: 1.66,
  /** Шаг и бег, м/с. */
  walk: 1.3,
  run: 2.4,
  /** Начальная скорость прыжка, м/с. */
  jump: 3.2,
  /** Обзор в покое и на бегу, градусы. */
  fov: 70,
  fovRun: 74,
  /**
   * Как быстро взгляд догоняет угол обзора, 1/с. Медленно: собранный на лампе
   * взгляд после появления раскрывается за пару секунд, а не рывком, и бег
   * у немолодого тела расширяет обзор не сразу.
   */
  fovRate: 1.5,
  /** Выносливость: пять секунд бега, десять секунд отдыха стоя. */
  stamina: { drain: 1 / 5, gainIdle: 1 / 10 },
} as const

export const LOOK = {
  /** Множитель дыхания камеры в покое. */
  breath: 1.3,
} as const
