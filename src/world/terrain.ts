/**
 * world/terrain.ts — высота земли формулой.
 *
 * Земля считается аналитически, а не деревом коллизий: формула точнее
 * треугольников и ничего не стоит при сборке. Опора тела (`support.ts`)
 * спрашивает её в каждой точке шага, внешние проверки - в каждой точке
 * маршрута.
 *
 * Пока поляна ровная. Волны грязи и ровная площадка у поста придут вместе с
 * самой поляной; сигнатура уже та, что им нужна.
 */

export function heightAt(_x: number, _z: number): number {
  return 0
}
